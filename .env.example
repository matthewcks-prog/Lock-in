# =============================================================================
# Lock-in - Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and will never be committed
#
# File hierarchy (Vite loads in this order):
# 1. .env.local (highest priority - your actual secrets, gitignored)
# 2. .env.development / .env.production (mode-specific defaults)
# 3. .env (lowest priority - shared defaults)
# =============================================================================

# -----------------------------------------------------------------------------
# EXTENSION BUILD CONFIGURATION (VITE_ prefix required)
# Used by Vite at build time - embedded in extension bundle
# -----------------------------------------------------------------------------

# Development Supabase (for local dev & testing)
VITE_SUPABASE_URL_DEV=https://your-dev-project.supabase.co
VITE_SUPABASE_ANON_KEY_DEV=your-dev-anon-key-here
VITE_BACKEND_URL_DEV=http://localhost:3000

# Production Supabase (for production builds)
VITE_SUPABASE_URL_PROD=https://your-prod-project.supabase.co
VITE_SUPABASE_ANON_KEY_PROD=your-prod-anon-key-here
VITE_BACKEND_URL_PROD=https://your-backend.azurecontainerapps.io

# Optional: Sentry error tracking
# VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Optional: Extension debug logging
# VITE_DEBUG=true

# -----------------------------------------------------------------------------
# BACKEND SERVER CONFIGURATION (NO VITE_ prefix)
# Used by Node.js backend at runtime - NOT embedded in extension
# -----------------------------------------------------------------------------

# Environment
NODE_ENV=development
PORT=3000

# Development Supabase (backend service role key)
SUPABASE_URL_DEV=https://your-dev-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY_DEV=your-dev-service-role-key-here
SUPABASE_ANON_KEY_DEV=your-dev-anon-key-here

# Production Supabase (backend service role key)
SUPABASE_URL_PROD=https://your-prod-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY_PROD=your-prod-service-role-key-here
SUPABASE_ANON_KEY_PROD=your-prod-anon-key-here

# AI Services (OpenAI)
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Optional: Azure OpenAI (requires quota approval)
# AZURE_OPENAI_API_KEY=your-azure-openai-key
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_API_VERSION=2024-02-01
# AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4-mini
# AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT=text-embedding-3-small

# Optional: Azure Speech (for transcription)
# AZURE_SPEECH_API_KEY=your-azure-speech-key
# AZURE_SPEECH_REGION=australiaeast
# AZURE_SPEECH_LANGUAGE=en-US

# Optional: Rate limiting
# DAILY_REQUEST_LIMIT=100
# CHAT_LIST_LIMIT=50
# MAX_CHAT_LIST_LIMIT=100
