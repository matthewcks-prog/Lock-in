# =============================================================================
# Lock-in MCP Server Configuration
# DO NOT commit this file with real credentials
# Copy to .env.local and fill in your values
# =============================================================================

# =============================================================================
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# =============================================================================

# Database Server Configuration (lockin-db-dev)
# Full CRUD access for MCP in development environment
# See tools/mcp/docs/SUPABASE_DEV_ADMIN_SETUP.md for setup instructions
LOCKIN_DB_CONNECTION_STRING_DEV=postgresql://dev_admin:your_password@db.uszxfuzauetcchwcgufe.supabase.co:5432/postgres
LOCKIN_DB_MAX_ROWS=1000
LOCKIN_DB_TIMEOUT_MS=5000
LOCKIN_DB_READONLY=false

# Supabase Project Reference (Dev)
LOCKIN_SUPABASE_PROJECT_REF_DEV=uszxfuzauetcchwcgufe
LOCKIN_SUPABASE_URL_DEV=https://uszxfuzauetcchwcgufe.supabase.co

# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================
# MCP should NEVER connect to production database
# These variables intentionally left empty as a security boundary
# All production writes go through backend API only

# LOCKIN_DB_CONNECTION_STRING_PROD=  # NEVER SET THIS
# LOCKIN_SUPABASE_PROJECT_REF_PROD=vtuflatvllpldohhimao  # Reference only, no connection
# LOCKIN_SUPABASE_URL_PROD=https://vtuflatvllpldohhimao.supabase.co  # Reference only

# ============================================================================
# Files Server Configuration (lockin-files)
# ============================================================================
# Repository file access (read-only, respects allowed/excluded paths)
# Use forward slashes for all paths (works on Windows and Unix)
LOCKIN_REPO_ROOT=C:/Users/matth/Lock-in
LOCKIN_EXTENSION_PATH=C:/Users/matth/Lock-in/extension
LOCKIN_CORE_PATH=C:/Users/matth/Lock-in/core
LOCKIN_API_PATH=C:/Users/matth/Lock-in/api
LOCKIN_DOCS_PATH=C:/Users/matth/Lock-in/docs
LOCKIN_INTEGRATIONS_PATH=C:/Users/matth/Lock-in/integrations

# ============================================================================
# Build Server Configuration (lockin-build)
# ============================================================================
# Safe build commands only (type-check, lint, test, verify-build)
LOCKIN_BUILD_TIMEOUT_MS=300000
LOCKIN_BUILD_MAX_OUTPUT_BYTES=1048576

# ============================================================================
# Docs Server Configuration (lockin-docs)
# ============================================================================
# Documentation search (indexed markdown files)
LOCKIN_DOCS_INDEX_PATH=C:/Users/matth/Lock-in/docs
