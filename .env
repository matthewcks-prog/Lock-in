# =============================================================================
# Lock-in - Shared Default Configuration
# =============================================================================
# This file contains safe default values that can be committed to git
# Override with .env.local for your actual secrets (gitignored)
# =============================================================================

# -----------------------------------------------------------------------------
# EXTENSION BUILD DEFAULTS (VITE_ prefix)
# These are placeholders - override in .env.local
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL_DEV=https://your-dev-project.supabase.co
VITE_SUPABASE_ANON_KEY_DEV=your-dev-anon-key
VITE_BACKEND_URL_DEV=http://localhost:3000

VITE_SUPABASE_URL_PROD=https://your-prod-project.supabase.co
VITE_SUPABASE_ANON_KEY_PROD=your-prod-anon-key
VITE_BACKEND_URL_PROD=https://lock-in-backend.australiaeast.azurecontainerapps.io

# -----------------------------------------------------------------------------
# BACKEND SERVER DEFAULTS (NO VITE_ prefix)
# These are placeholders - override in .env.local or backend/.env
# -----------------------------------------------------------------------------
PORT=3000
