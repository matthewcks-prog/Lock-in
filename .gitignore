# Dependencies
node_modules/

# Environment variables
# Follow hierarchy: .env.local (secrets) > .env.development/.env.production (mode defaults) > .env (safe defaults)
# Only .env.local and other sensitive env files are gitignored
.env.local
.env.*.local
.env.production.local
.env.development.local

# COMMITTED (safe defaults only, no secrets):
# .env - Safe default values
# .env.example - Template with documentation
# .env.example.local - Extended backend template
# .env.development - Development mode defaults
# .env.production - Production mode defaults

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Operating System
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
*.swp
*.swo
*~

# VS Code (user-specific settings and MCP configs)
.vscode/*
!.vscode/mcp.json.template
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/launch.json

# Cursor IDE (user-specific MCP configs)
.cursor/mcp.json

# Build outputs
dist/
build/
!build/
build/*
!build/viteShared.ts
extension/dist/
extension/ui/
extension/libs/
*.zip

# Chrome Extension (packed)
*.crx
*.pem

# Temporary files
tmp/
temp/
*.tmp

# Testing
coverage/
.nyc_output/
