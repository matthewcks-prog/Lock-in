# Dependencies
node_modules/

# Workspace lock files (only root should have package-lock.json)
backend/package-lock.json
**/package-lock.json
!package-lock.json

# Environment variables
# All .env files should be ignored to prevent accidental secret commits
.env
.env.*
!.env.example
!.env.template

# COMMITTED (templates and examples only - no secrets):
# .env.example - Root-level template with documentation
# .env.template - Alternative template format
# backend/.env.example - Backend-specific template

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Operating System
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
*.swp
*.swo
*~

# VS Code (user-specific settings and MCP configs)
.vscode/*
!.vscode/mcp.json.template
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/launch.json

# Cursor IDE (user-specific MCP configs)
.cursor/mcp.json

# Build outputs
dist/
build/
!build/
build/*
!build/viteShared.ts
extension/dist/
extension/ui/
extension/libs/
*.zip

# Chrome Extension (packed)
*.crx
*.pem
extension/config.js
extension/config.js.map

# Temporary files
tmp/
temp/
*.tmp

# Testing
coverage/
.nyc_output/
