#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🎨 Running pre-commit checks (formatting & linting)..."

# Run lint-staged (will format and lint staged files)
npx lint-staged

# Get exit code
status=$?

if [ $status -ne 0 ]; then
  echo "❌ Pre-commit checks failed. Please review and fix the issues."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
exit 0
