# =============================================================================
# Lock-in - Development Mode Defaults
# =============================================================================
# Loaded automatically when running with NODE_ENV=development or --mode development
# Override with .env.local for actual secrets
# =============================================================================

# -----------------------------------------------------------------------------
# EXTENSION BUILD (VITE_ prefix)
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL_DEV=https://your-dev-project.supabase.co
VITE_SUPABASE_ANON_KEY_DEV=your-dev-anon-key
VITE_BACKEND_URL_DEV=http://localhost:3000

# Enable debug logging in development
# VITE_DEBUG=true

# -----------------------------------------------------------------------------
# BACKEND SERVER (NO VITE_ prefix)
# -----------------------------------------------------------------------------
PORT=3000

SUPABASE_URL_DEV=https://your-dev-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY_DEV=your-dev-service-role-key
SUPABASE_ANON_KEY_DEV=your-dev-anon-key

OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o-mini

# Development rate limits (more permissive)
DAILY_REQUEST_LIMIT=1000
CHAT_LIST_LIMIT=50
MAX_CHAT_LIST_LIMIT=100
