# npm configuration for Lock-in monorepo

# Package lock
save-exact=false
package-lock=true

# Legacy peer dependencies
legacy-peer-deps=false

# Workspace configuration
# All workspace operations run from root
